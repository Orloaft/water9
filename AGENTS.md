# Project Anchor — water9

**You are working on `water9`** (`/mnt/nxt-dev/water9`). This is the active project.

## PRIME DIRECTIVE — your location is the truth
Your active project is **the repository your current working directory is in** — derived
from `pwd`, never from memory. After ANY compaction or context loss, before you edit,
preview, screenshot, or commit:

1. Run `pwd` and `git rev-parse --show-toplevel`.
2. Treat that repo as the project you are working on.
3. If it doesn't match the task you were given, **STOP and ASK** — do not guess.

Do **NOT** drift to TIB (`/mnt/nxt-dev/tib`) or any other project unless the task
explicitly names it. Daily memory notes and `MEMORY.md` are cross-project *history*,
not your current assignment. When in doubt, this file and your `pwd` win.
